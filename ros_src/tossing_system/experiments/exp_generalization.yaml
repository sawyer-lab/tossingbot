name: exp_generalization
description: "Test generalization: train on simple, test on complex"

training:
  objects:
    - cube
    - cylinder
    - bar
    - sphere
  steps: 1500

evaluation:
  objects:
    - L_shape
    - I_shape
    - T_shape
    - C_shape
    - cross
  episodes: 100
